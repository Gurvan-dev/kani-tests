# TODO

- Try to find out what goes from instant for basic memset to 2min on a `sb`
  memset
- It seems like both memset9 and memset10 take just as long to work, could
  either be bitvectors or just taking the global pointer?
